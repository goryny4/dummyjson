{
  "$schema": "https://sharedb.org/v1/resource.schema.json",
  "name": "stock",
  "kind": "table",
  "title": "Stock",
  "source": {
    "type": "query",
    "query": "select stock from share-db/sharedb-test/products WHERE price < (select count(*) from share-db/sharedb-test/products/posts) AND category = 'beauty' LIMIT 3 OFFSET 2"
  },
  "fields": [
    {
      "name": "stock",
      "type": "integer"
    }
  ]
}
